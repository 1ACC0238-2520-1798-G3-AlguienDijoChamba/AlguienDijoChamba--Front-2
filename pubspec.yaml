name: alguiendijochamba_app_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  persistent_bottom_nav_bar: ^6.2.1
  http: ^1.5.0
  flutter_bloc: ^9.1.1
  easy_load_more: ^2.0.0
  shared_preferences: ^2.5.3
  equatable: ^2.0.7
  dartz: ^0.10.1
  image_picker: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
